// TODO(store-launch): replace with real listing URLs once the app is
// published to each store — tracked as an open item in the workspace's
// build-log.md. Until then these are placeholders; the download banner and
// "take a reading in the app" prompt both link out to them regardless, so
// they're non-functional stubs, not broken links to a real listing.
const appStoreUrl = 'https://apps.apple.com/app/idTODO';
const playStoreUrl = 'https://play.google.com/store/apps/details?id=TODO';
